# Sourced by the interactive shell, early. Runs in the shell's own process:
# exports and functions persist, and a syntax error breaks the user's shell.
#
# Typical contents: proxy variables, company shell init, private aliases.

# export https_proxy=http://proxy.internal:8080
# export no_proxy=.internal,localhost
# [[ -f /opt/company/rc/shell.sh ]] && source /opt/company/rc/shell.sh
