# Entry point for personal kitty overrides (included from kitty.conf).
# Splits into a shared file plus a platform-specific file picked at load
# time by select-platform-conf.sh.
include jhou-common.conf
geninclude select-platform-conf.sh
